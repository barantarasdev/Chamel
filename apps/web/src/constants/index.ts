//Also need to change for $avatar-size variable in (apps/web/src/styles/variables.scss)
export const GENERAL_ICON_SIZE = 62;

export const GENERAL_FONT_CONFIG = {
  subsets: ['latin'],
  weight: ['300', '400', '500'],
};

export const DEFAULT_LANGUAGE = 'en';

export const MESSAGE_INPUT_MAX_LENGTH = 500;
export const MESSAGE_INPUT_MIN_LENGTH = 1;
