{
  "name": "rules",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "rules/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/rules",
        "main": "rules/src/index.ts",
        "tsConfig": "rules/tsconfig.lib.json",
        "assets": ["rules/*.md"]
      }
    }
  }
}
